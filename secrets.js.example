// ElevenLabs API Credentials
// Copy this file to secrets.js and add your actual secrets
// secrets.js is gitignored, so your secrets won't be committed

const ELEVENLABS_SECRETS = {
  apiKey: 'sk_your_api_key_here',
  agentId: 'your_agent_id_here',  // Optional
  voiceId: 'your_voice_id_here'   // Optional
};

// Export for use in extension
if (typeof window !== 'undefined') {
  window.ELEVENLABS_SECRETS = ELEVENLABS_SECRETS;
}





